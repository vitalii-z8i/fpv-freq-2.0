type FPVChannel = { id: string, channel: string, frequency: number }
type FPVHarmonica = { ctrlName: string, harmonicas: Array<number, number>[] }

type VTXTable = { key: string, name: string, cli?: string[], json?: string }
